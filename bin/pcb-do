#!/bin/sh
# pcbridge action shell -- a local visual agent calls this from Bash.
# The logic lives in `pcbridge/cli/do.py`; this only finds the venv.
set -eu
SELF=$(readlink -f "$0")
ROOT=$(dirname "$(dirname "$SELF")")
exec "$ROOT/.venv/bin/python" -m pcbridge.cli.do "$@"
